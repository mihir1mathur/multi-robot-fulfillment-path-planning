"""Reproducible measurement of the path planners.

This package contains no algorithm code. It only builds deterministic
scenarios, runs the existing planners over them, and aggregates what actually
happened. Every number it reports is measured, never assumed.
"""
