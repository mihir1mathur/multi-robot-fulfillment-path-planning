[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -v --tb=short
markers =
    integration: tests that require a real PostgreSQL (skipped unless TEST_DATABASE_URL is set)
filterwarnings =
    ignore::DeprecationWarning:starlette.testclient
